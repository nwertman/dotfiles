# ~/.tmuxinator/trunk.yml
# you can make as many tabs as you wish...

project_name: trunk
project_root: ~/Source/Professional/IntegrationTools/trunk
pre: sudo ls /tmp && svn update
tabs:
  - Vim: vim
  - Pry: pry
  - bash:
  - bash:
  - bash:
  - bash:
  - bash:
  - bash:
  - bash:
  - 'System Status':
      layout: even-vertical
      panes:
        - top -u -s 3
        - sudo tail -f /var/log/system.log
