# ~/.tmuxinator/change-object.yml
# you can make as many tabs as you wish...

project_name: change_object
project_root: ~/Source/Professional/IntegrationTools/change_object
pre: sudo ls /tmp && svn update
tabs:
  - Vim: vim
  - Pry: pry
  - bash:
  - bash:
  - bash:
  - bash:
  - bash:
  - bash:
  - bash:
  - Status:
      layout: even-vertical
      panes:
        - top -u -s 3
        - sudo tail -f /var/log/system.log
